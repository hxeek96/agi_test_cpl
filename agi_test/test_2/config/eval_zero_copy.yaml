# FPGA Offload Evaluation Config
name: eval_zero_copy

generator:
  max_tokens: 4096
  dtype: fp32
  max_gen_len: 64    # ← 추가
  # attn_impl: sdpa  # ← 이거 추가

harness:
  tasks:
    - hellaswag
    - piqa
    - task: nq_open
      num_fewshot: 5
  limit: 5000

