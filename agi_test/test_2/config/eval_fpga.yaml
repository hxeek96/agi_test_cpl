# FPGA Offload Evaluation Config
name: fpga_eval

generator:
  max_tokens: 2
  dtype: fp16
  max_gen_len: 1    # ← 추가
  # attn_impl: sdpa  # ← 이거 추가

harness:
  tasks:
    - hellaswag
    - piqa
    - task: nq_open
      num_fewshot: 5
  limit: 100


